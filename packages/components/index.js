/*
 * Image
 * Adds a button to pick an image/video, for use in heroes and similar blocks
 *
 * Required attributes: media (object), focalPoint(object)
 * Props: attributes, setAttributes, isFocus, hasCaption(optional)
 */
/**
 * Internal dependencies
 */
import Image from './Image';

/*
 * Linkable
 * A simplified button with link
 */
import Linkable from './Linkable';

/*
 * SimpleButton
 * A simplified button with link
 */
import SimpleButton from './SimpleButton';

/* eslint-disable */
export { Image, SimpleButton, Linkable };
